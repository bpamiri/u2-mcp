# U2 MCP Server Configuration
# Copy this file to .env and fill in your credentials

# =============================================================================
# Database Connection (Required)
# =============================================================================

# Universe/UniData server hostname or IP address
U2_HOST=your-server.example.com

# Username for authentication
U2_USER=your-username

# Password for authentication
U2_PASSWORD=your-password

# Account name to connect to
U2_ACCOUNT=YOUR_ACCOUNT

# =============================================================================
# Database Connection Options (defaults shown)
# =============================================================================

# Service type: uvcs (Universe) or udcs (UniData)
# U2_SERVICE=uvcs

# Server port
# U2_PORT=31438

# Enable SSL/TLS connection
# U2_SSL=false

# Connection timeout in seconds
# U2_TIMEOUT=30

# =============================================================================
# Safety Settings (defaults shown)
# =============================================================================

# Disable all write operations
# U2_READ_ONLY=false

# Maximum records returned by SELECT queries
# U2_MAX_RECORDS=10000

# Maximum seconds for a query to execute before timeout
# U2_QUERY_TIMEOUT=60

# Comma-separated list of blocked TCL commands
# U2_BLOCKED_COMMANDS=DELETE.FILE,CLEAR.FILE,CNAME,CREATE.FILE

# =============================================================================
# HTTP Server Configuration (for --http or --streamable-http modes)
# =============================================================================

# Host to bind HTTP server to
# U2_HTTP_HOST=0.0.0.0

# Port for HTTP server
# U2_HTTP_PORT=8080

# Comma-separated list of allowed CORS origins, or * for all
# U2_HTTP_CORS_ORIGINS=*

# =============================================================================
# OAuth Authentication (for Claude.ai Integrations with --streamable-http)
# =============================================================================

# Enable OAuth authentication for Streamable HTTP endpoint
# U2_AUTH_ENABLED=false

# OAuth issuer URL (this server's public URL)
# U2_AUTH_ISSUER_URL=https://u2-mcp.example.com

# =============================================================================
# Identity Provider Settings (when auth is enabled)
# =============================================================================

# Identity provider type: 'duo', 'auth0', or 'oidc'
# U2_IDP_PROVIDER=oidc

# OIDC discovery URL (.well-known/openid-configuration)
# U2_IDP_DISCOVERY_URL=

# Client ID for external identity provider
# U2_IDP_CLIENT_ID=

# Client secret for external identity provider
# U2_IDP_CLIENT_SECRET=

# Scopes to request from external IdP (space-separated)
# U2_IDP_SCOPES=openid profile email

# =============================================================================
# Duo-specific Settings (when U2_IDP_PROVIDER=duo)
# =============================================================================

# Duo API hostname (e.g., api-XXXXXXXX.duosecurity.com)
# U2_DUO_API_HOST=

# =============================================================================
# Token Settings (defaults shown)
# =============================================================================

# Access token expiry time in seconds (1 hour)
# U2_TOKEN_EXPIRY_SECONDS=3600

# Refresh token expiry time in seconds (30 days)
# U2_REFRESH_TOKEN_EXPIRY_SECONDS=2592000

# =============================================================================
# Connection Watchdog (defaults shown)
# =============================================================================

# Enable connection health monitoring watchdog
# U2_WATCHDOG_ENABLED=true

# Watchdog check interval in seconds
# U2_WATCHDOG_INTERVAL=30

# Timeout for health check operations in seconds
# U2_WATCHDOG_TIMEOUT=10

# Max consecutive failures before forcing reconnect
# U2_WATCHDOG_MAX_FAILURES=3

# =============================================================================
# Audit Logging (defaults shown)
# =============================================================================

# Enable audit logging of all MCP tool calls
# U2_AUDIT_ENABLED=false

# Directory path for audit log files
# U2_AUDIT_PATH=/var/log/u2-mcp/audit

# Include tool results in audit logs (may contain sensitive data)
# U2_AUDIT_INCLUDE_RESULTS=true

# Maximum characters to log for tool results (truncates if larger)
# U2_AUDIT_MAX_RESULT_SIZE=10000
